# Memory review presentation

English | [中文](2026-09-14-memory-review-layout.zh.md)

The memory review dialog sizes the outer Modal card, not only its clipped child. Suggestions hide recall/search telemetry and records without candidate text by default. An explicit technical-record toggle reveals retained records without invoking review or deleting data. Host acceptance policy is unchanged. The three memory scopes remain separate.

Validation: focused memory component tests; browser layout requires live verification after client bundle refresh.
