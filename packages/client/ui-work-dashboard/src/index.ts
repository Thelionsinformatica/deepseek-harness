/**
 * Leon Work dashboard plugin, node half. The Host mounts this empty body so
 * the browser half follows the configured plugin lifecycle.
 */

/** Host plugin body — no Host-side behavior for the dashboard plugin. */
export function apply(): void {}
